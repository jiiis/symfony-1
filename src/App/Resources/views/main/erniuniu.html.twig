{%- extends 'App::layout.html.twig' -%}

{%- set session_name = app.request.query.get('session_name') -%}

{%- block content -%}
    <div>
        <p>This is erNiuNiu!!!</p>
        <p>
            <a href="{{ path('home') }}">Home</a>
        </p>

        <p>Session: {{ session_name }} => {{ app.session.get(session_name) }}</p>
    </div>
{%- endblock -%}
